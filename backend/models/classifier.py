"""Loads the fine-tuned EfficientNetB0 model, exposes predict()."""
# import tensorflow as tf
# model = tf.keras.models.load_model("classifier.h5")

def predict(image_array):
    # TODO: run model.predict, return (category, confidence)
    pass
